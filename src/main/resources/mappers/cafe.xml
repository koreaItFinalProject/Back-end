<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finalProject.Back.repository.CafeMapper">

    <insert id="save">
        insert into cafe (id , ownerId, cafeName, address, lat, lng, category)
        values (default, #{ownerId}, #{cafeName} , #{address}, #{lat} , #{lng} , " ")
    </insert>
    <select id="findByCategory" resultType="com.finalProject.Back.entity.Cafe">
        SELECT *
        FROM cafe
        WHERE category = #{category} OR #{category} = '전체'
    </select>
    <select id="findBySearchAndCategory" resultType="com.finalProject.Back.entity.Cafe">
        SELECT *
        FROM cafe
        WHERE (category = #{category} OR #{category} = '전체')
        AND cafeName LIKE CONCAT('%', #{search}, '%')
    </select>
</mapper>